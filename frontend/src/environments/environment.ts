// environment.ts (dev)
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',  // your dev backend URL (without /auth)
  authApiUrl: 'http://localhost:8080/auth'  // for login endpoints
};
